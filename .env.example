## Example environment variables for remote monitoring and training

# Remote host in user@host form
REMOTE_HOST=ubuntu@your.remote.host

# Optional SSH key path (if not using agent)
SSH_KEY_PATH=~/.ssh/id_ed25519

# SSH options (override cautiously in production)
# SSH_OPTS=-o BatchMode=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=10 -o ServerAliveInterval=15 -o ServerAliveCountMax=4

# TensorBoard port and logdir on remote
TB_PORT=6006
TB_LOGDIR=artifacts/m7c_125m/tb

# Data/cache locations
HF_HOME=/mnt/hf_home
HF_DATASETS_CACHE=/mnt/hf_home/datasets
NSA_DATA_CACHE=artifacts/hf_cache

# Watchdog
NSA_WATCH_HALT=1

