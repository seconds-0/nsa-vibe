# Core
torch==2.3.*
# Triton not used in M0 (reserved for later milestones)
triton==3.* ; platform_system == "Linux"
# Optional FA-2 (M1+); guarded imports (avoid on macOS)
flash-attn==2.* ; platform_system == "Linux"
# Tooling
numpy
hydra-core
pydantic
pytest
hypothesis
ruff
mypy
